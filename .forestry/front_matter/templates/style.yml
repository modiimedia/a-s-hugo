---
label: Style
hide_body: true
is_partial: false
fields:
- type: field_group
  label: Site Colors
  name: site_colors
  fields:
  - type: color
    name: main
    config:
      color_format: Hex
      required: true
    label: main Color
    description: Choose your site's main color
  - type: color
    config:
      color_format: Hex
      required: false
    label: Main Dark
    name: background
    description: Choose a dark version of the main colo
  - type: color
    config:
      color_format: Hex
      required: true
    label: Accent Color
    name: accent
    description: Choose an accent color for your site
  - type: color
    config:
      color_format: Hex
      required: true
    label: Text Color
    name: text_color
data_files:
- data/style.yaml
