---
label: Style
hide_body: true
is_partial: false
fields:
- type: field_group
  label: Site Colors
  name: site_colors
  fields:
  - type: color
    name: primary
    config:
      color_format: Hex
      required: true
    label: Primary Color
    description: Choose your site's main color
  - type: color
    config:
      color_format: Hex
      required: true
    label: Primary Dark Color
    name: background
    description: Main color to be used on dark backgrounds
  - type: color
    config:
      color_format: Hex
      required: true
    label: Secondary Color
    name: secondary
    description: Choose an accent color for your site
  - type: color
    config:
      color_format: Hex
      required: true
    label: Text Color
    name: text_color
    description: Color of the text
data_files:
- data/style.yaml
